#include <iostream>
using namespace std;
int sumar(int a, int b) {
    return a + b;
}
int main() {
    int a, b;
    cout << "Ingrese dos numeros: ";
    cin >> a >> b;    
    cout << "La suma es: " << sumar(a, b) << endl;    
    return 0;
}
