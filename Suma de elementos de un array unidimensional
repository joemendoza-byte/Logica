#include <iostream>
#include <vector>
using namespace std;
int main() {
    vector<int> A = {10, 20, 30, 40, 50};
    int suma = 0;
    for (int i = 0; i < A.size(); i++) {
        suma += A[i];
    }
    cout << "Suma de elementos: " << suma << endl;
    return 0;
}
